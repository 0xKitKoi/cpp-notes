#include <stdio.h>
#include <string.h>
#include <fstream>

int main (int argc, char *argv[])
{
	char url[100] "http://scuzzy.ml"
    std::fstream fs;
    fs.open(url);
    fs.close();

    return 0;
	
}
